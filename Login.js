// Efek sederhana untuk animasi teks berjalan (opsional tambahan)
document.addEventListener("DOMContentLoaded", () => {
  const movingText = document.getElementById("movingText");
  movingText.style.animationPlayState = "running";
});